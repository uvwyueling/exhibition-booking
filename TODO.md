# 展厅预约系统 · 待办清单

## 已跑通（背景）

- 前端 `useBackend: true`，运行在真后端上
- 边缘函数：读档期 / 写预约（写入前查冲突；展期 ≤10 天自助确认、>10 天转审核）
- 飞书自动化：预约 >10 天 → 通知我进表人工审核
- SCF 定时：状态=已确认 → 发确认信给学生 + 回写标记（≤10 天自助确认的也一并覆盖）

---

## A. 核心功能（待建）

- [ ] **前三天《登记表》邮件** — 每天定时 SCF：扫「布展日 = 今天+3 且 状态=已确认 且 前三天邮件已发≠true」→ 生成填好的 `.docx` → 发到班长（联系人邮箱）→ 回写「前三天邮件已发=true」。*依赖 .docx 模板。*
- [ ] **驳回邮件** — 扩展现有 SCF：扫「状态=已驳回 且 驳回邮件已发≠true」→ 发重约信 → 回写。需在飞书表新增复选框字段「驳回邮件已发」。*（档期转绿已自动生效，无需额外处理。）*

## B. 待确认 / 待提供

- [ ] **确认域名是否持久** — 现用链接是自定义域名（备案 / 海外区），还是 3 小时限时预览？预览会失效、不能发给学生。**优先级最高。**
- [ ] **《登记表》.docx 模板定稿** — 删掉押金，留占位符：班级 / 展览名称 / 场地 / 布展日 / 撤展日 / 天数 / 联系人 / 电话 / 指导教师。*（阻塞 A 的前三天邮件。）*
- [ ] **两张场地照片** — 有图后把 URL 填进 `index.html` 顶部的 `CONFIG.venueImages`（当前为占位框）。

## C. 打磨（可选）

- [ ] **确认信文案** — 现写「已通过管理员审核」；≤10 天自助确认并未经人工审核，措辞略不准。可改中性（「你的预约已确认」）或按是否人工审核分两版。
- [ ] **抄送自己** — 给 SCF 配环境变量 `NOTIFY_CC=03024@huznu.edu.cn`，每封确认信抄送一份，实时掌握每一单成约。
- [ ] **笔误** — `package.json` 描述里的「浙大 SMTP」应为「网易企业邮箱」。

## D. 验证

- [ ] ≤10 天的自助预约，学生**是否真收到确认信**？（确认信约 5 分钟一轮、非秒发，确认时延可接受。）
- [ ] 飞书里**手填**的大展日期，月历显示**是否对得上、没差一天**？（差则调 `_feishu.js` 里的 `TZ_MS`。）

---

**优先级**：A 两块 + B 域名最要紧；C、D 锦上添花。
建议从「前三天《登记表》邮件」起步 —— 唯一还没自动化、且最省事的一环，只等你的 .docx 模板。
